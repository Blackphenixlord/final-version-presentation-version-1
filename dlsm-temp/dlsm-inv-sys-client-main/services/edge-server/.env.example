# ── Edge-server environment ──────────────────────

# -- Mode --
NO_DB=1

# -- Network --
PORT=8080
HOST=0.0.0.0

# -- Tailscale --
# TAILSCALE_HOST=my-laptop
# TAILSCALE_HOST=100.100.100.100

# -- CORS --
CORS_ORIGINS=*

# -- Static frontend --
# SERVE_STATIC=0
# STATIC_DIR=/custom/path/to/dist

# -- PostgreSQL (only when NO_DB is not 1) --
PGHOST=localhost
PGPORT=5432
PGUSER=aether
PGPASSWORD=aether
PGDATABASE=aether